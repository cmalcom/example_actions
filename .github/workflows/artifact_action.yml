name: artifact_action

on: [push]

env:
  ARTIFACT_NAME: myartifact

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Upload the artifact
        uses: actions/upload-artifact@v2
        with: 
          name: ${{ env.ARTIFACT_NAME }}
          path: .
      - name: Notify Teams
        uses: FTsbrown/msteams-action@master
        with: 
          TITLE: Test GitHub Action Notification  # the message title
          BODY: 🎉🎉 YOU HAVE BEEN NOTIFIED 🎉🎉  # the body of the message
          MS_TEAMS_WEBHOOK: https://collegeboardorg.webhook.office.com/webhookb2/d61535e3-7f30-4a5c-b0cf-7f83cb3b95a8@7530bded-fd6e-4f58-b5d2-ea681eb07663/IncomingWebhook/c064993e08b4469588e4ee7813472390/13ec3f2a-3423-4f1d-b805-847884fe3f1b
