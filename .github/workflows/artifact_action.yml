name: artifact_action

on: [push]

env:
  ARTIFACT_NAME: myartifact

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the code
        uses: actions/checkout@v2
      - name: Upload the artifact
        uses: actions/upload-artifact@v2
        with: 
          name: ${{ env.ARTIFACT_NAME }}
          path: .
      - name: Notify Teams
        uses: FTsbrown/msteams-action@master
        with: 
          TITLE: Test GitHub Action Notification
          BODY: 🎉🎉 YOU HAVE BEEN NOTIFIED 🎉🎉  with compliments - ${{ env.GITHUB_ACTOR }} commit - ${{ env.GITHUB_SHA }}
          MS_TEAMS_WEBHOOK: ${{ secrets.TEAMS_WEBHOOK_URL }}
